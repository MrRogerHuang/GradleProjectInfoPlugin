initscript {
    dependencies {
        classpath files("${pluginRootDir}/build/libs/GradleProjectInfoPlugin-${version}.jar")
    }
}

allprojects {
    apply plugin: com.github.MrRogerHuang.GradleProjectInfoPlugin
}